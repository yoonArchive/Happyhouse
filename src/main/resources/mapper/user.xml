<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.UserMapper">
	
	<insert id="registerUser" parameterType="user">
		insert into user (userId, userPwd, userName, email, phone, joinDate)
		values (#{userId}, #{userPwd}, #{userName}, #{email}, #{phone}, now())
	</insert>

	<select id="idCheck" parameterType="string" resultType="int">
		select count(userId)
		from user
		where userId = #{userId}
	</select>
	
	<select id="login" parameterType="user" resultType="user">
		select *
		from user
		where userId=#{userId} and userPwd=#{userPwd}
	</select>
	
	<select id="findPwd" parameterType="user" resultType="string">
		select userPwd
		from user
		where userId=#{userId} and userName=#{userName} and phone=#{phone}
	</select>
	
	<update id="updateUser" parameterType="user">
		update user
		set userPwd=#{userPwd}, userName=#{userName}, email=#{email}, phone=#{phone}
		where userId=#{userId}
	</update>
	
	<select id="selectById" parameterType="String" resultType="user">
		select *
		from user
		where userId=#{userId}
	</select>
	
	<delete id="deleteById" parameterType="string">
		delete from user
		where userId=#{userId}
	</delete>
</mapper>